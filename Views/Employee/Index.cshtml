@model System.Data.DataTable;
@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
   
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Name
            </th>
            <th>
               Age
            </th>
            <th>
                Salary
            </th>
            <th>
                Department
            </th>
            <th>
                Gender
            </th>
            <th>
                <a asp-action="AddorEdit">Add New</a>
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@for(int i=0;i<Model.Rows.Count;i++)
{
        <tr>
            <td>
                    @Model.Rows[i]["emp_Name"]
            </td>
            <td>
                    @Model.Rows[i]["emp_Age"]
            </td>
            <td>
                    @Model.Rows[i]["emp_Salary"]
            </td>
            <td>
                    @Model.Rows[i]["emp_Department"]
            </td>
            <td>
                    @Model.Rows[i]["emp_Gender"]
            </td>
            <td>
                    <a asp-action="AddorEdit" asp-route-id="@Model.Rows[i]["emp_ID"]">Edit</a> |
              
                    <a asp-action="Delete" asp-route-id="@Model.Rows[i]["emp_ID"]">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
